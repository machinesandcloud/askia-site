{
  "name": "askia-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --config=.eleventy.js",
    "build": "eleventy --config=.eleventy.js",
    "serve": "eleventy --serve --config=.eleventy.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
