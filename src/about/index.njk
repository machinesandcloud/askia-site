---
layout: layout.njk
title: About
description: "Meet the team and the method behind Askia."
metaTitle: "About Askia | Career Coaching Built for Signal"
metaDescription: "Learn how Askia helps tech professionals build clear signal, stronger interviews, and better offers."
breadcrumbs:
  - name: "Home"
    url: "/"
  - name: "About"
    url: "/about/"
---
<section class="about-hero-new">
  <div class="container about-hero-grid">
    <div class="about-hero-copy">
      <p class="hero-eyebrow">About Askia</p>
      <h1 class="about-hero-title">{{ about.hero.title }}</h1>
      <p class="about-hero-subtitle">{{ about.hero.subtitle }}</p>
      <p class="about-hero-supporting">{{ about.story.body }}</p>
      <div class="hero-cta">
        <a class="btn btn-primary" href="{{ about.hero.ctaLink }}">{{ about.hero.ctaLabel }}</a>
        <a class="btn btn-ghost" href="/services/">Explore services</a>
      </div>
      <span class="hero-proof">{{ about.hero.proof }}</span>
      <div class="about-hero-badges">
        <span>Signal clarity</span>
        <span>Interview execution</span>
        <span>Offer strategy</span>
      </div>
    </div>
    <div class="about-hero-panel">
      <div class="about-hero-stats">
        {% for stat in about.stats %}
          <div class="about-stat">
            <strong>{{ stat.value }}</strong>
            <span>{{ stat.label }}</span>
          </div>
        {% endfor %}
      </div>
      <div class="about-hero-system">
        <strong>The Askia system</strong>
        <div class="about-hero-steps">
          <div><span>Signal</span><p>Define role + level signal with clarity.</p></div>
          <div><span>Momentum</span><p>Build assets that generate interviews.</p></div>
          <div><span>Conversion</span><p>Execute interviews and offers with leverage.</p></div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="section about-story-new">
  <div class="container about-story-grid">
    <div>
      <p class="hero-eyebrow">Why we exist</p>
      <h2 class="section-title section-title-glow">{{ about.story.title }}</h2>
      <p class="section-subtitle">{{ about.story.body }}</p>
    </div>
    <div class="about-values">
      {% for value in about.values %}
        <div class="value-card">
          <h3>{{ value.title }}</h3>
          <p>{{ value.body }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section about-method">
  <div class="container about-method-grid">
    <div>
      <p class="hero-eyebrow">How we work</p>
      <h2 class="section-title section-title-glow">A repeatable path to momentum</h2>
      <p class="section-subtitle">Every engagement is built around the same three moves.</p>
    </div>
    <div class="about-method-cards">
      <div class="method-card">
        <span>01</span>
        <strong>Signal</strong>
        <p>Clarify role, level, and positioning so your story reads senior.</p>
      </div>
      <div class="method-card">
        <span>02</span>
        <strong>Momentum</strong>
        <p>Create assets and outreach that generate interviews fast.</p>
      </div>
      <div class="method-card">
        <span>03</span>
        <strong>Conversion</strong>
        <p>Interview and offer strategy that closes with leverage.</p>
      </div>
    </div>
  </div>
</section>

<section class="section about-founder">
  <div class="container founder-grid">
    <div class="founder-photo">
      <img src="{{ about.founder.photo }}" alt="{{ about.founder.name }}" />
    </div>
    <div class="founder-copy">
      <p class="hero-eyebrow">Founder</p>
      <h2 class="section-title section-title-glow">{{ about.founder.name }}</h2>
      <p class="section-subtitle">{{ about.founder.role }}</p>
      <p>{{ about.founder.body }}</p>
      <div class="founder-badges">
        <span>Hiring‑table insight</span>
        <span>Offer strategy</span>
        <span>Execution coaching</span>
      </div>
    </div>
  </div>
</section>

<section class="section about-credibility">
  <div class="container">
    <div class="team-header">
      <p class="hero-eyebrow">Trust</p>
      <h2 class="section-title section-title-glow">Built with real hiring‑table context</h2>
      <p class="section-subtitle">We’ve helped candidates across top tech orgs.</p>
    </div>
    <div class="about-logo-row">
      <img src="/assets/logos/testimonials/google.svg" alt="Google logo" />
      <img src="/assets/logos/testimonials/microsoft.svg" alt="Microsoft logo" />
      <img src="/assets/logos/testimonials/figma.svg" alt="Figma logo" />
      <img src="/assets/logos/testimonials/shopify.svg" alt="Shopify logo" />
      <img src="/assets/logos/testimonials/uber.svg" alt="Uber logo" />
    </div>
  </div>
</section>

<section class="section about-team">
  <div class="container">
    <div class="team-header">
      <p class="hero-eyebrow">Team</p>
      <h2 class="section-title section-title-glow">The people behind the outcomes</h2>
      <p class="section-subtitle">A lean team of specialists focused on signal, strategy, and execution.</p>
    </div>
    <div class="team-grid">
      {% for person in about.team %}
        <div class="team-card">
          <img src="{{ person.photo }}" alt="{{ person.name }}" />
          <strong>{{ person.name }}</strong>
          <span>{{ person.role }}</span>
          <p>{{ person.bio }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container cta-band">
    <div>
      <h2 class="section-title">{{ about.cta.title }}</h2>
      <p class="section-subtitle">{{ about.cta.body }}</p>
    </div>
    <a class="btn btn-primary" href="{{ about.cta.ctaLink }}">{{ about.cta.ctaLabel }}</a>
  </div>
</section>
