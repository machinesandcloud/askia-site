---
layout: layout.njk
title: Services
description: "Askia coaching services to land top tech roles."
---
<section class="hero">
  <div class="container hero-grid">
    <div>
      <h1>{{ services.hero.title }}</h1>
      <p>{{ services.hero.subtitle }}</p>
      <a class="btn btn-primary" href="{{ services.hero.ctaLink }}">{{ services.hero.ctaLabel }}</a>
    </div>
    <div class="card">
      <strong>What you'll get</strong>
      <p>Focused, high-touch coaching that makes your next move clear and achievable.</p>
      <ul class="highlight-list">
        <li>Role targeting and market positioning</li>
        <li>Resume + LinkedIn optimization</li>
        <li>Interview prep and negotiation</li>
      </ul>
    </div>
  </div>
</section>

<section class="section">
  <div class="container grid-2">
    {% for item in services.services %}
      <div class="card">
        <h3 class="section-title">{{ item.title }}</h3>
        <p>{{ item.body }}</p>
        <ul class="highlight-list">
          {% for bullet in item.bullets %}
            <li>{{ bullet }}</li>
          {% endfor %}
        </ul>
      </div>
    {% endfor %}
  </div>
</section>

<section class="section">
  <div class="container">
    <h2 class="section-title">{{ services.faq.title }}</h2>
    <div class="faq">
      {% for item in services.faq.items %}
        <div class="faq-item">
          <strong>{{ item.question }}</strong>
          <p>{{ item.answer }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container card">
    <h2 class="section-title">{{ services.cta.title }}</h2>
    <p class="section-subtitle">{{ services.cta.body }}</p>
    <a class="btn btn-primary" href="{{ services.cta.ctaLink }}">{{ services.cta.ctaLabel }}</a>
  </div>
</section>
