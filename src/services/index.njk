---
layout: layout.njk
title: Services
description: "Askia coaching services to land top tech roles."
---
<section class="hero">
  <div class="container hero-grid">
    <div>
      <p class="hero-eyebrow">Services</p>
      <h1 class="hero-title">
        <span class="hero-title-lead">{{ services.hero.title }}</span>
      </h1>
      <p class="hero-subtitle">{{ services.hero.subtitle }}</p>
      <a class="btn btn-primary" href="{{ services.hero.ctaLink }}">{{ services.hero.ctaLabel }}</a>
    </div>
    <div class="card">
      <strong>What you get</strong>
      <p>Every package is designed to elevate your market signal and help you land better offers — faster.</p>
      <ul class="highlight-list">
        <li>Role targeting and positioning</li>
        <li>Story + brand alignment</li>
        <li>Interview and offer strategy</li>
      </ul>
    </div>
  </div>
</section>

<section class="section">
  <div class="container tier-grid">
    {% for tier in services.tiers %}
      <div class="tier-card{% if tier.tag == 'Most Popular' %} tier-featured{% endif %}">
        <div class="tier-header">
          <span class="tier-tag">{{ tier.tag }}</span>
          <h3>{{ tier.title }}</h3>
        </div>
        <p>{{ tier.body }}</p>
        <ul>
          {% for bullet in tier.bullets %}
            <li>{{ bullet }}</li>
          {% endfor %}
        </ul>
      </div>
    {% endfor %}
  </div>
</section>

<section class="section">
  <div class="container">
    <h2 class="section-title section-title-glow">{{ services.faq.title }}</h2>
    <div class="faq-grid">
      {% for item in services.faq.items %}
        <details class="faq-card">
          <summary>{{ item.question }}</summary>
          <p>{{ item.answer }}</p>
        </details>
      {% endfor %}
    </div>
  </div>
</section>

<section class="section">
  <div class="container cta-band">
    <div>
      <h2 class="section-title">{{ services.cta.title }}</h2>
      <p class="section-subtitle">{{ services.cta.body }}</p>
    </div>
    <a class="btn btn-primary" href="{{ services.cta.ctaLink }}">{{ services.cta.ctaLabel }}</a>
  </div>
</section>
